package backend.database.columns;

/* Add comments */
public enum UserTableCells
{
    userId, primary_role, secondary_role, tertiary_role, quaternary_role, firstName, lastName, email, phone, address, username, password
}
